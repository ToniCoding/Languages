/**
    We can define variables to store data that can be used in other parts of the code. It can be useful when we need to use some data not right after obtainment.
    We can use it later by writing the name of the variable without quotation marks and/or parenthesis.
    Syntax: def <variableName> = <variableValue>
*/

def username = "TestUser"
def password = 1234

// Print the variable names.
println(username)
println(password)